window.SIDEBAR_ITEMS = {
    fn: [
        [
            "mask",
            "Mask for a single [`SyntaxKind`] in a `TokenSet`. This operation is used to efficiently store a set of [`SyntaxKind`]s in a [`TokenSet`].",
        ],
    ],
    struct: [
        [
            "TokenSet",
            "A memory-efficient bit-set for managing and querying a collection of [`SyntaxKind`]s / [`TokenKind`]s.",
        ],
    ],
};
